// variable that will point to a date object
const today = new Date()

// today. // note: this will show all the available things off a Date

const person = {
	age: 20
}

class Color {
}

const red = new Color()
